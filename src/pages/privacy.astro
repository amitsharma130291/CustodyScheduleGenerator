---
import BaseLayout from '../layouts/BaseLayout.astro';
import Navbar from '../components/Navbar.astro';
import Footer from '../components/Footer.astro';
import Breadcrumbs from '../components/seo/Breadcrumbs.astro';
import JsonLd from '../components/seo/JsonLd.astro';
import { getCanonicalUrl } from '../lib/seo/metadata';
import { buildBreadcrumbSchema, buildWebPageSchema } from '../lib/seo/schema';

const title = 'Privacy Policy for CustodyBuilder Tools | CustodyBuilder';
const description =
	'Read the CustodyBuilder privacy policy for custody schedule tools, calendar templates, local schedule data, analytics use, privacy, and updates.';
const canonical = '/privacy';
const canonicalUrl = getCanonicalUrl(canonical);
const breadcrumbs = [
	{ name: 'Home', url: getCanonicalUrl('/') },
	{ name: 'Legal', url: getCanonicalUrl('/disclaimer') },
	{ name: 'Privacy Policy', url: canonicalUrl },
];
const schema = [
	buildBreadcrumbSchema(breadcrumbs),
	buildWebPageSchema({ name: title, description, url: canonicalUrl }),
];
---

<BaseLayout title={title} description={description} canonical={canonical}>
	<JsonLd slot="head" data={schema} />
	<Navbar />
	<Breadcrumbs items={breadcrumbs} />
	<main>
		<section class="container-shell py-12 lg:py-16">
			<div class="mx-auto max-w-3xl">
				<a href="/" class="text-sm font-medium text-muted transition-colors duration-200 hover:text-primary">← Back to CustodyBuilder</a>
				<p class="mt-5 text-xs font-semibold uppercase tracking-[0.18em] text-muted">Legal</p>
				<h1 class="mt-4 text-5xl font-semibold leading-[0.98] tracking-[-0.055em] text-primary lg:text-6xl">
					Privacy Policy
				</h1>
				<p class="mt-6 text-lg leading-8 text-secondary">
					CustodyBuilder is an educational planning tool for creating custody schedules, parenting time calendars, and printable planning materials.
				</p>
			</div>

			<div class="premium-card mx-auto mt-8 grid max-w-3xl gap-6 rounded-[1.75rem] p-6 text-base leading-8 text-secondary sm:p-8">
				<section>
					<h2 class="text-2xl font-semibold tracking-[-0.04em] text-primary">Information We May Collect</h2>
					<p class="mt-3">
						CustodyBuilder may collect basic usage data in the future, such as page views, device information, browser type, and general interaction patterns, if privacy-friendly analytics are added.
					</p>
				</section>

				<section>
					<h2 class="text-2xl font-semibold tracking-[-0.04em] text-primary">Schedule Data</h2>
					<p class="mt-3">
						Any schedule data entered into CustodyBuilder is used to generate calendars, percentages, print views, and PDF exports. Some data, such as theme preference or share-link values, may be stored locally in your browser through local storage or URL parameters.
					</p>
				</section>

				<section>
					<h2 class="text-2xl font-semibold tracking-[-0.04em] text-primary">Analytics</h2>
					<p class="mt-3">
						CustodyBuilder may use privacy-friendly analytics in the future to understand site usage and improve the product.
					</p>
				</section>

				<section>
					<h2 class="text-2xl font-semibold tracking-[-0.04em] text-primary">Personal Information</h2>
					<p class="mt-3">
						We do not sell personal information.
					</p>
				</section>

				<section>
					<h2 class="text-2xl font-semibold tracking-[-0.04em] text-primary">Policy Updates</h2>
					<p class="mt-3">
						This privacy policy may be updated periodically as CustodyBuilder evolves.
					</p>
				</section>
			</div>
		</section>
	</main>
	<Footer />
</BaseLayout>
